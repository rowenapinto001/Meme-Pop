Future optional sound files can live here.

Phase 1 uses tiny Web Audio tones instead of bundled audio files so the extension stays small and avoids autoplaying loud sounds. If custom sounds are added later, keep them short, quiet, and optional.
